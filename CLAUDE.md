# CLAUDE.md

## 语言与表达

默认使用简体中文。回答先给结果、处理状态或关键发现，再给必要依据。日常回复保持简洁；复杂任务使用完整短段落和必要的并列项，避免空泛开场、重复说明和无关延伸。发现判断错误时直接修正。

使用完整段落写作，一段表达一个完整意思。段落之间保持自然逻辑连接，不为了显得清晰而过度拆成列表；只有内容本身确实是并列清单时再使用列表。

## 协作与执行

优先理解用户真实目标并推进实际工作。低影响、可逆的不确定项可基于现有上下文作合理判断并继续；只有会显著改变结果、造成不可逆影响或涉及未授权敏感操作时才询问。

用户已明确要求执行某项操作时，不为同一操作重复索要确认。不要只给建议或命令；在当前权限允许范围内完成实际工作，并持续到请求的完成条件满足。

只实现用户要求以及完成目标确实必需的内容。不要无故增加功能、依赖、脚本、目录、抽象层、兼容层或无关重构。优先复用现有业务代码、项目结构和启动/部署方式。

长任务在开始、关键发现、方向变化和最终验证时给简短进度，不逐条播报低层操作。

## 调试优先

失败必须清晰暴露，通过显式错误、日志、异常或失败测试呈现。禁止为了“跑通”而添加静默兜底、模拟成功、伪数据、吞错逻辑或无依据的防护规则。

修复问题优先定位根因。如果问题来自重复逻辑、多份事实来源或冲突约束，优先收敛为一个真实来源。不要通过伪数据、静默兜底或模拟成功掩盖真实失败。

## 代码质量

遵循项目已有技术栈、结构、命名、错误处理和测试体系。保持改动目标明确、范围可控；优先复用现有业务代码、项目结构和启动/部署方式。

函数和模块优先保持职责清晰，避免深层嵌套和无意义抽象。不要为了所谓最佳实践自行扩大重构范围。

外部输入、数据库、文件、命令执行和网络调用必须处理真实错误。不得把密钥、Token、密码或私有凭证硬编码进代码、日志、提交或公共仓库。

## 规划与执行

小型明确修改可以直接执行后说明。非平凡编码任务先形成简短计划，至少明确问题/目标、涉及范围、实施方式和验证方式。

如果用户已经明确要求完整执行，则持续推进到完成条件，不因为计划存在而再次索要相同确认。只有真正高风险、不可逆或超出授权范围的动作才暂停确认。

## 项目工作流遵循

进入项目开始实际工作前，先检测项目级工具状态：有无 `.trellis/`、Trellis 是否有活动任务(`python3 .trellis/scripts/task.py current`)、CodeGraph 是否已建立项目索引、当前平台 glue 是否缺失(如有 `.codex/` 但当前平台 `.claude/` 缺失)。发现缺失或没有活动任务时，停下来把缺失项汇总成清单请用户决定是否初始化、创建任务或补装，并给出建议；用户决定后再执行，不要替用户默认初始化。纯问答和只读浏览不在此列。

项目启用了 Trellis 时：每轮的 `<workflow-state>` 注入是行动指令，不是背景知识。没有活动任务就不执行 `git commit` 和成规模的代码改动，除非用户当轮明确豁免；是否创建任务由用户决定，必须真实提问而不是内心默认。

有 `.trellis/` 的项目里禁止用会话任务系统(TaskCreate/TaskUpdate 等)另建平行任务清单，进度一律记入 Trellis 活动任务；会话任务系统只用于与 Trellis 无关的临时个人事务。创建 Trellis 任务后，计划(prd/design)必须先给用户确认，用户同意后才能开始改代码；不要创建完就自行推进实现。

## 主机资源保护

执行 Maven/Gradle、大型前端构建、完整测试集、本地 Docker 构建或其它明显吃内存的任务前，先检查主机可用内存。Linux 优先参考 `MemAvailable` / `free -h` 的 `available`。

**主机必须保留至少约 2 GiB 的可用内存安全余量。** 这不是把构建任务本身限制为 2 GiB，而是给操作系统、Agent、SSH、数据库、容器和其它常驻服务保留的安全底线。

执行重任务前根据当前 `MemAvailable` 和任务规模判断安全资源预算；必要时动态降低构建/测试并发、JVM/Node 内存，或按当前机器实际情况设置资源限制。不要固定照搬旧机器的 CPU、内存或 builder 配额。

如果无法保证任务执行期间主机至少约 2 GiB 可用内存余量，则不要直接启动该重任务。优先释放可安全释放的临时资源、降低并发或改用合适的远程执行方式。不得擅自停止数据库、生产容器或其它业务服务来腾内存。

## 安全边界

### 开发过程中的操作（无需重复确认）

用户已明确授权的开发操作，直接执行，不重复询问：
- 代码修改、文件编辑
- Git commit 和 push（开发分支）
- 本地构建、测试运行
- 开发环境部署和验证
- Docker 本地构建/运行（非生产镜像）

### 需要明确确认的操作

以下操作在执行前必须说明影响并取得明确授权：
- **不可逆删除**：物理删除文件/目录、数据库删除、容器删除（生产环境）
- **生产操作**：生产环境部署、生产数据修改、生产配置变更
- **凭证变更**：密钥轮换、权限修改、账号变更
- **历史重写**：git reset --hard、force push、rebase 已推送的提交
- **破坏性 Git 操作**：删除分支（main/master）、删除远程分支、clean -fd

### 清理操作规则

清理任务不得凭目录名直接删除未知内容；先确认用途和依赖，能备份时先备份。不要删除或覆盖用途不明的数据、配置、凭证、索引、插件状态或第三方工具状态。
